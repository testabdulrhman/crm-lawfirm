[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# دعم توجيه SPA (احتياطي إضافة إلى public/_redirects)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
